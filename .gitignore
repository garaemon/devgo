devgo
# `go build ./tools/...` writes this binary at the repository root; every make
# target uses `go run`. The leading slash keeps the pattern off the
# tools/covreport/ directory.
/covreport

# Coverage artifacts written by the make coverage targets.
coverage*.out
coverage*.html
base-coverage.out
